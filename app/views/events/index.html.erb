
<h1>All events</h1>
<h2>You have total <%= @events.count %> events</h2>
<% @events.each do |event|%>
<li><%= event.data %></li>
<button>click here to parse</button>
<button>delete </button>

<% end %>

<br/>
<br/>
<br/>
<br/>

<%= button_to 'add a new data', new_event_path, method: :get %>